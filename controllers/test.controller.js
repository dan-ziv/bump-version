app.controller('testController', ['$scope', '$location', '$timeout', 'GitHub', 'Jira', 'User', 'Confluence',
    function ($scope, $location, $timeout, GitHub, Jira, User, Confluence) {
        $scope.onClick = function () {
        };
    }]);